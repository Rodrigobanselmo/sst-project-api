const MB = 1024 * 1024;
export const MAX_IMAGE_SIZE = 5 * MB;
export const MAX_DOCUMENT_SIZE = 5 * MB;

export const IMAGE_ALLOWED_TYPES = /(jpg|jpeg|png|webp|jpe|bmp|raw)$/;
export const IMAGE_AND_PDF_ALLOWED_TYPES = /(jpg|jpeg|png|webp|jpe|bmp|raw|pdf)$/;
export const CERTIFIED_ALLOWED_TYPES = /(pfx|perm|cer|crt)$/;
