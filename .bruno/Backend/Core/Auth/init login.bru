meta {
  name: init login
  type: http
  seq: 2
}

post {
  url: {{url}}/{{version}}/init-login
  body: json
  auth: none
}

headers {
  X-Institution-Id: 2
}

body:json {
  {
      "access": "muryel.cabral@coalasaude.com.br",
      "sendToken": true
  }
}

tests {
  const r = res.getBody()
  bru.setEnvVar("token", r.accessToken)
  
}
