meta {
  name: update role
  type: http
  seq: 2
}

put {
  url: {{url}}/{{version}}/users/:userId/roles/:roleId
  body: json
  auth: bearer
}

params:path {
  userId: 15
  roleId: 706
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
      "profileId": 1,
      "companyPositionId": 2, 
      "class": "ok"
  }
}
