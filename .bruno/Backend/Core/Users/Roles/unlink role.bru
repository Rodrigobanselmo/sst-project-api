meta {
  name: unlink role
  type: http
  seq: 4
}

delete {
  url: {{url}}/{{version}}/users/:userId/roles/:roleId
  body: json
  auth: bearer
}

params:path {
  userId: 15
  roleId: 788
}

auth:bearer {
  token: {{token}}
}
