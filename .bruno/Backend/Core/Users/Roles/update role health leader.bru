meta {
  name: update role health leader
  type: http
  seq: 3
}

put {
  url: {{url}}/{{version}}/users/:userId/roles/:roleId/health-leader
  body: json
  auth: bearer
}

params:path {
  userId: 15
  roleId: 15
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
      "isHealthLeader": true
  }
}
