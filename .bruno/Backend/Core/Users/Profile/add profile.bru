meta {
  name: add profile
  type: http
  seq: 1
}

post {
  url: {{url}}/{{version}}/profiles
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
      "name": "Perfil Teste",
      "permissions": [2,3,4,5,17],
      "institutionTypeId": 78,
      "type": "NORMAL"
  }
}
